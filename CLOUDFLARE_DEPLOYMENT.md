# 🚀 คู่มือการติดตั้งและ Deploy GIO Cafe & Slow Life ขึ้น Cloudflare

คู่มือฉบับนี้จะช่วยคุณนำเว็บแอปพลิเคชันบัญชีรายวันขึ้นโฮสติ้ง **Cloudflare Pages** และเชื่อมต่อระบบฐานข้อมูลคลาวด์ **Cloudflare D1 (SQLite)** ฟรีตลอดชีพ!

---

## 🛠️ วิธีที่ 1: ติดตั้งผ่านหน้าเว็บไซต์ Cloudflare & GitHub (ง่ายที่สุด - แนะนำ)

วิธีนี้ไม่ต้องติดตั้งโค้ดเครื่องมือใดๆ ในเครื่องคอมพิวเตอร์ของคุณ เพียงแค่มีบัญชี GitHub และ Cloudflare เท่านั้น

### ขั้นตอนที่ 1: อัปโหลดโค้ดขึ้น GitHub
1. สร้าง **GitHub Repository** ใหม่ (ตั้งชื่อ เช่น `gio-cafe-slowlife`) ที่ [github.com](https://github.com/)
2. อัปโหลดไฟล์ทั้งหมดในโฟลเดอร์โครงการนี้ขึ้นไปยัง Repository ดังกล่าว

### ขั้นตอนที่ 2: สร้างฐานข้อมูล Cloudflare D1 (Database)
1. เข้าสู่ระบบ [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. ในเมนูด้านซ้าย ไปที่ **Workers & Pages** > **D1**
3. คลิก **Create database** > เลือก **Dashboard**
4. ตั้งชื่อฐานข้อมูลว่า: `gio-cafe-db` แล้วคลิก **Create**
5. เมื่อสร้างเสร็จแล้ว ให้คลิกที่ชื่อฐานข้อมูล `gio-cafe-db` แล้วไปที่แท็บ **Console**
6. คัดลอกข้อความคำสั่ง SQL จากไฟล์ [schema.sql](file:///Users/m/Documents/ร้านค้า/schema.sql) ในโฟลเดอร์นี้ ไปวางในช่อง Console แล้วกด **Execute** เพื่อสร้างตารางบันทึกข้อมูล

### ขั้นตอนที่ 3: สร้างโครงการ Pages และเชื่อม GitHub
1. ไปที่เมนูด้านซ้าย **Workers & Pages** > **Overview**
2. คลิก **Create application** > เลือกแท็บ **Pages** > คลิก **Connect to Git**
3. ล็อกอินบัญชี GitHub และเลือก Repository `gio-cafe-slowlife` ที่สร้างไว้
4. ในส่วน **Build settings**:
   - **Framework preset**: เลือก `None`
   - **Build command**: ปล่อยว่างไว้
   - **Build output directory**: ปล่อยว่างหรือใส่ `/`
5. คลิก **Save and Deploy** จากนั้นระบบจะสร้างลิงก์เว็บไซต์หน้าตาประมาณ `gio-cafe-slowlife.pages.dev` ให้คุณทันที!

### ขั้นตอนที่ 4: ผูกฐานข้อมูล D1 เข้ากับแอป Pages (สำคัญมาก)
1. ในหน้าโครงการ Pages ที่เพิ่งสร้างเสร็จ ไปที่แท็บ **Settings** > เลือกเมนู **Functions**
2. เลื่อนลงมาด้านล่างหาหัวข้อ **D1 database bindings**
3. คลิก **Add binding** (เลือกทำในส่วน Production และ Preview) แล้วกรอกข้อมูลดังนี้:
   - **Variable name**: กรอกเป็นอักษรตัวใหญ่ว่า `DB` (สำคัญมาก: ห้ามสะกดเป็นคำอื่น)
   - **D1 database**: เลือกชื่อฐานข้อมูล `gio-cafe-db` ที่สร้างไว้
4. คลิก **Save**
5. **ทำการสั่ง Deploy ใหม่เพื่ออัปเดตการผูกตาราง**: ไปที่แท็บ **Deployments** > เลือกการอัปโหลดล่าสุด > คลิกปุ่มจุดสามจุด (...) ด้านขวา > เลือก **Redeploy**

---

## 💻 วิธีที่ 2: ติดตั้งผ่าน Command Line (Wrangler CLI)

หากต้องการทำรายการและอัปโหลดโดยใช้โปรแกรมเทอร์มินัลผ่าน Wrangler บน Mac:

### ขั้นตอนที่ 1: ติดตั้ง Node.js
* ดาวน์โหลดและติดตั้ง **Node.js** รุ่นล่าสุดจาก [nodejs.org](https://nodejs.org/) (ซึ่งจะได้คำสั่ง `npx` และ `npm` มาพร้อมกัน)

### ขั้นตอนที่ 2: ล็อกอิน Cloudflare บน Terminal
* เปิดโปรแกรม Terminal และรันคำสั่ง:
  ```bash
  npx wrangler login
  ```
  *(ระบบจะเด้งหน้าเว็บบราวเซอร์ขึ้นมาเพื่อให้คุณกดยืนยันตัวตนเข้าระบบ)*

### ขั้นตอนที่ 3: สร้างฐานข้อมูลและสร้างตาราง
1. สร้าง D1 Database บน Cloudflare:
   ```bash
   npx wrangler d1 create gio-cafe-db
   ```
2. สร้างโครงสร้างตารางข้อมูลโดยใช้ไฟล์ SQL:
   ```bash
   npx wrangler d1 execute gio-cafe-db --remote --file=schema.sql
   ```

### ขั้นตอนที่ 4: อัปโหลดเว็บไซต์ขึ้น Cloudflare Pages
* สั่ง Deploy โฟลเดอร์ปัจจุบันขึ้นไปยังระบบ Pages:
  ```bash
  npx wrangler pages deploy . --project-name=gio-cafe-slowlife
  ```
  *(หลังจากเสร็จสิ้นแล้ว ให้เข้าไปที่ Cloudflare Dashboard เพื่อตั้งค่า D1 Database Bindings ชื่อ `DB` ตามหัวข้อ "วิธีที่ 1 ขั้นตอนที่ 4" เพื่อผูกโครงการเข้ากับฐานข้อมูล)*

---

## 🔒 ฟังก์ชัน Hybrid (การทำงานของระบบสำรองข้อมูล)

แอปพลิเคชันนี้ออกแบบระบบซิงค์ข้อมูลไฮบริดอัจฉริยะ (Offline + Online) ให้เสร็จสมบูรณ์แล้ว:
* **เมื่อรันอยู่บนโฮสติ้ง Cloudflare Pages**: เว็บแอปจะสลับเป็น **โหมดเชื่อมต่อคลาวด์ (Cloud Sync Mode)** ดึงและบันทึกข้อมูลหลักลงฐานข้อมูล D1 โดยตรง ทำให้ข้อมูลอัปเดตข้ามอุปกรณ์ได้ทันที (เช่น เปิดใช้งานผ่านคอมพิวเตอร์และมือถือพร้อมกัน)
* **เมื่อรันแบบออฟไลน์/เปิดจากคอมพิวเตอร์เปล่าๆ**: แอปจะสลับไปเป็น **โหมดออฟไลน์ (Offline Mode)** ดึงข้อมูลจาก LocalStorage ในเครื่อง เพื่อป้องกันการเกิดข้อผิดพลาดทำให้ใช้งานแอปพลิเคชันได้ตามปกติโดยไม่มีอาการค้าง
* **การซิงค์ครั้งแรกอัตโนมัติ**: เมื่อเปิดใช้งาน Cloud Sync Mode ครั้งแรก หากแอปตรวจพบว่าฐานข้อมูลคลาวด์ยังว่างเปล่าอยู่ แต่ในบราวเซอร์ของคุณมีข้อมูลบันทึกรายวันเดิมค้างไว้ ระบบจะสั่งอัปโหลดแบ็กอัปยอดเดิมทั้งหมดขึ้นฐานข้อมูลคลาวด์ให้คุณโดยอัตโนมัติทันที!
